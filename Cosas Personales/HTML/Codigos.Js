//name of codes JavaScript

rect(x, y, w, h);   //and sometimes the radius
ellipse(x, y, w, h);
triangle(x1, y1, x2, y2, x3, y3);  //each tip requires the X and Y that is why it is 3 times X and Y
line(x1, y1, x2, y2);
Point(x, y);
arc(x, y, w, h, start, stop)    //You can make circles and many things!
quad(x1, y1, x2, y2, x3, y3, x4, y4)   //it is very difficult to do this

















